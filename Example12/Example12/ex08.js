/**
 * 콜백함수 
 * 콜백함수(callback function)는 다른 함수에 
 * 파라미터로 전달되어 이후에 어떤 이벤트가 발생했거나 
 * 특정 작업이 완료된 후에 호출되는 함수를 말한다 
 * 
 * * 자바스크립트에서 비동기 처리를 위해 사용되며 
 * 특히 이벤트 핸들링 이나 AJAX 요청 등에 사용된다 
 */

setTimeout(function () {
    console.log("Hello World!!")
}, 3000); // 1초당 1000밀리세컨드 



